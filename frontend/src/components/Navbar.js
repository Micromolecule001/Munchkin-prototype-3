import { Link } from 'react-router-dom';

const Navbar = () => {

    return (
        <header>
            <div className="container">
                <h1 className="logo">Munchkin</h1>
            </div>
        </header>
    );
}

export default Navbar;